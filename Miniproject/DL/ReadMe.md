<h4>If implementing first time run following two commands in terminal</h4>

### OpenCV:
```bash
pip install opencv-python
```
### argparse:
```bash
pip install argparse
```

## Then just run the python file 'detect.py' from terminal
